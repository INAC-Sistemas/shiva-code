# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

/src/generated/prisma

# O .gitignore da raiz ignora `lib/` por causa dos builds do harness, e o padrão
# alcança `src/lib/` daqui — que é código-fonte. Sem esta reinclusão, um arquivo
# novo em src/lib fica de fora do commit em silêncio e a aplicação quebra para
# todo mundo que clona.
!/src/lib/

{
  "name": "dsh-tool-guard",
  "version": "0.2.2",
  "description": "Tool-seam guard for dsh: each role writes only its own surface (the product is everything outside mds/, prototype/, testes/ and .git/), and no agent may set a ticket's status to done",
  "type": "module",
  "license": "MIT",
  "main": "lib/index.js",
  "exports": {
    ".": "./lib/index.js",
    "./package.json": "./package.json"
  },
  "engines": {
    "node": ">=20"
  },
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  },
  "scripts": {
    "test": "node --test test/*.test.mjs"
  },
  "files": [
    "LICENSE",
    "README.md",
    "cordis.patch.yml",
    "lib"
  ]
}

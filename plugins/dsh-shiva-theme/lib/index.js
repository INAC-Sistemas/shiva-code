// dsh-shiva-theme host half: appearance only, so the node half is an empty
// apply — its existence puts the row in the host cordis.yml and Loader while
// every behaviour lives in the browser bundle (lib/client.js).
export function apply() {}
